{
  "name": "kbeats",
  "version": "1.0.0",
  "description": "K Beats - Music Production Landing Page with AI Chatbot",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "install-all": "npm install --legacy-peer-deps && cd frontend && npm install --legacy-peer-deps && cd ../api && npm install",
    "dev:frontend": "cd frontend && npm start",
    "dev:api": "cd api && npm run dev",
    "build:frontend": "cd frontend && npm run build",
    "build:api": "cd api && npm run build"
  }
}
